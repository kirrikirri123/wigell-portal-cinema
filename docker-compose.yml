services:
  mysql:
    image: mysql
    container_name: wigell-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: keycloak
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: keycloak
      MYSQL_PASSWORD: password
    ports:
      - "6606:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  #Keycloak
  keycloak:
    image: quay.io/keycloak/keycloak:26.5.7
    container_name: wigell-keycloak
    restart: unless-stopped
    command: start-dev
    environment:
      KC_DB: mysql
      KC_DB_URL: jdbc:mysql://mysql:3306/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: password
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    ports:
      - "8081:8080"
    depends_on:
      - mysql
volumes:
  mysql_data:




    #Samma på alla microtjänster. FÖr allas möjligheter att köra docker likadant.